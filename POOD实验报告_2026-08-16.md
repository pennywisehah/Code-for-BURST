# CIFAR10–STL10 POOD 联邦遗忘攻击实验报告

## 1. 实验目的

本组实验为模拟实验，主要研究 POOD（Poisoning Out-of-Distribution Data）数据的数量、重复次数与样本多样性对联邦模型及目标样本的影响。实验并不以验证生产级机器遗忘合规性为主要目的，而是观察以下过程：

1. 使用代理模型检索并优化与目标样本特征接近的 STL10 POOD 数据；
2. 将 POOD 注入指定联邦客户端并完成 Non-IID 联邦训练；
3. 使用 FedEraser 遗忘注入记录；
4. 比较遗忘前后全局模型、目标样本、目标类别、POOD 标签类别和遗忘集的变化。

本报告分析 2026 年 8 月 15 日得到的 6 组结果，重点回答两个问题：

- 提高 `poison_repeats` 是否会持续增强攻击效果？
- 在总注入量相同时，提高 POOD 样本多样性是否优于重复少量样本？

## 2. 公共实验设置

六组实验除 POOD 数量参数外，其余主要配置保持一致。

| 配置项 | 设置 |
|---|---|
| 联邦训练集 | CIFAR10 |
| POOD 候选集 | STL10 test |
| 模型 | ResNet18（512维倒数第二层特征） |
| 代理模型 | CIFAR10、Dirichlet `alpha=1.0` 联邦模型 |
| 代理模型准确率 | 81.92% |
| 客户端划分 | Dirichlet Non-IID，`alpha=1.0` |
| 客户端数量 | 10 |
| 恶意客户端 | Client 6 |
| 联邦轮数 | 50 |
| 本地训练 | 1 epoch，SGD，学习率 0.01，batch size 128 |
| 目标样本 | CIFAR10 test index 25，`bird(2)` |
| 候选数量 | 5000 |
| Top-K | 200 |
| POOD来源与标签 | STL10 `dog(5)`，注入标签为 CIFAR10 `dog(5)` |
| 扰动 | 200步，学习率 0.005，L∞上限 8/255 |
| FedEraser | `delta_t=2`，25个历史快照 |

代理模型检索得到的前5张 POOD 在各次实验中保持一致，源索引为：

```text
1232, 5784, 5942, 1869, 7972
```

其扰动前平均目标特征相似度约为 0.8892；对于 `p=5` 的实验，扰动后相似度稳定在约 0.9568–0.9570，说明代理模型中的 POOD 优化过程较稳定。

## 3. 实验编号与参数映射

为便于比较，按照注入规模重新编号如下。

| 实验 | 源文件 | 不同POOD数 `p` | `poison_repeats` | 总注入记录数 |
|---|---|---:|---:|---:|
| E1 | `summary(2).json` | 5 | 20 | 100 |
| E2 | `summary(1).json` | 5 | 50 | 250 |
| E3 | `summary.json` | 5 | 75 | 375 |
| E4 | `summary(4).json` | 5 | 100 | 500 |
| E5 | `summary(3).json` | 5 | 200 | 1000 |
| E6 | `summary(5).json` | 10 | 50 | 500 |

E1–E5用于分析固定 `p=5` 时重复次数的影响；E4和E6总注入量均为500，用于比较“少量样本高重复”和“更多样本低重复”。

## 4. 全局模型与遗忘集结果

| 实验 | 全局ACC（前） | 全局ACC（后） | 全局下降 | 遗忘集ACC（前→后） |
|---|---:|---:|---:|---:|
| E1：p5/r20 | 81.45% | 72.82% | 8.63 pp | 100% → 0% |
| E2：p5/r50 | 82.04% | 71.74% | 10.30 pp | 100% → 20% |
| E3：p5/r75 | 82.27% | 70.64% | 11.63 pp | 100% → 20% |
| E4：p5/r100 | 81.77% | 70.87% | 10.90 pp | 100% → 20% |
| E5：p5/r200 | 81.43% | 71.09% | 10.34 pp | 100% → 20% |
| E6：p10/r50 | 82.06% | 71.30% | 10.76 pp | 100% → 20% |

主要观察：

1. 六组实验遗忘前的全局准确率处于 81.43%–82.27%，联邦训练结果整体接近。
2. `delta_t=2` 时遗忘后准确率保持在 70.64%–72.82%，未出现此前 `delta_t=5` 时约31.79%的严重崩溃，但仍存在 8.63–11.63 个百分点的全局性能损失。
3. 增加重复次数没有造成全局准确率单调下降。E3的全局下降最大，但继续增加到 E4、E5后并未继续扩大。
4. 遗忘集准确率在所有实验中均显著下降，说明删除 POOD 记录确实改变了模型对这些样本的预测。不过，遗忘集是否降至0%不应单独作为遗忘质量或攻击强度的判据。

## 5. 目标样本攻击效果

| 实验 | bird置信度（前） | bird置信度（后） | 置信度下降 | 遗忘后预测 | 遗忘后Top1–Top2 margin | 攻击成功 |
|---|---:|---:|---:|---|---:|---|
| E1：p5/r20 | 84.94% | 26.57% | 58.37 pp | bird(2) | 1.06 pp | 否 |
| E2：p5/r50 | 82.53% | 36.57% | 45.95 pp | bird(2) | 10.34 pp | 否 |
| E3：p5/r75 | 68.54% | 24.76% | 43.78 pp | dog(5) | 0.63 pp | **是** |
| E4：p5/r100 | 85.22% | 25.75% | **59.48 pp** | truck(9) | 0.67 pp | **是** |
| E5：p5/r200 | 39.29% | 41.51% | **-2.23 pp** | bird(2) | 20.38 pp | 否 |
| E6：p10/r50 | 70.23% | 35.53% | 34.70 pp | bird(2) | 7.71 pp | 否 |

### 5.1 重复次数与攻击效果不是单调关系

`poison_repeats` 从20提高到100时，多数组合都对目标置信度产生了明显影响，其中 E3和E4成功改变目标标签，E4的置信度下降达到59.48个百分点，是本组实验中最强的结果。

然而，重复次数提高到200后，目标 `bird` 置信度反而从39.29%升至41.51%，margin扩大到20.38个百分点，攻击作用几乎消失。该结果说明：简单复制相同的5张 POOD 只能增加样本权重，不能持续增强 POOD 与目标之间的语义联系。

可能机制包括：

- 模型开始记忆高度重复的5张 `dog` 图片，而不是学习可迁移至目标 `bird` 的局部特征；
- 大量 `dog` 标签梯度强化了 POOD 自身分类边界，但没有继续增强目标样本对 POOD 的依赖；
- 恶意客户端数据量和本地 SGD 步数同时增加，使整个联邦训练轨迹发生变化；
- 过度重复使最终联邦模型的特征表示偏离用于生成 POOD 的代理模型特征空间。

### 5.2 E3和E4攻击成功，但结果接近决策边界

E3和E4遗忘后的 Top1–Top2 margin 分别只有0.63和0.67个百分点。两组确实满足当前代码的攻击成功定义，即遗忘前预测正确、遗忘后预测错误，但预测翻转幅度很小，可能对训练随机性敏感。

因此，这两组可视为有效的攻击现象，但在尚未完成多随机种子复现前，不宜表述为稳定攻击成功。

### 5.3 E1虽未翻转标签，但已非常接近成功

E1遗忘后仍预测为 `bird`，但 bird 置信度从84.94%下降至26.57%，Top1–Top2 margin只有1.06个百分点。这说明二元指标 `attack_success=false` 会掩盖明显的目标性能退化。后续报告应同时呈现标签翻转、置信度下降和margin变化。

## 6. 类别级性能变化

| 实验 | 目标类bird ACC下降 | POOD标签dog ACC下降 | 下降最大的类别 |
|---|---:|---:|---|
| E1：p5/r20 | 11.5 pp | 7.4 pp | deer：20.7 pp |
| E2：p5/r50 | 9.3 pp | 3.2 pp | deer：22.3 pp |
| E3：p5/r75 | 11.8 pp | 9.6 pp | deer：29.9 pp |
| E4：p5/r100 | 12.8 pp | 3.0 pp | deer：26.0 pp |
| E5：p5/r200 | 6.8 pp | 5.4 pp | deer：26.0 pp |
| E6：p10/r50 | **13.3 pp** | 8.7 pp | deer：29.8 pp |

类别级结果说明：

1. 单个目标样本的置信度变化远大于整个 `bird` 类别的变化，当前攻击呈现明显的样本级特征，而不是对整个目标类别的稳定破坏。
2. `dog` 类并没有在所有实验中成为下降最大的类别，表明删除 POOD 的影响并非简单等同于删除一批普通 dog 样本。
3. `deer` 类在六组实验中始终是最敏感类别，下降20.7–29.9个百分点。这可能来自 Non-IID 客户端分布、FedEraser校准偏差或 CIFAR10类别间特征关系，需要通过无POOD对照实验判断是否属于攻击特有现象。
4. E5的目标类下降仅6.8个百分点，为所有实验最低，与单目标攻击失效一致，再次表明重复次数过高并未带来更强的目标影响。

## 7. 固定总注入量下的样本数量对比

E4和E6均注入500条记录：

| 指标 | E4：5张×100次 | E6：10张×50次 |
|---|---:|---:|
| 扰动后代理特征相似度 | **0.9569** | 0.9400 |
| 最终特征距离 | **4.6333** | 5.4454 |
| 全局ACC下降 | 10.90 pp | 10.76 pp |
| 目标置信度下降 | **59.48 pp** | 34.70 pp |
| 目标类ACC下降 | 12.8 pp | **13.3 pp** |
| 攻击成功 | **是** | 否 |

在当前“共享一个通用扰动”的实现下，E4明显优于E6。原因是同一个通用扰动需要同时拉近更多图片时，优化难度增加：`p=10` 的最终平均特征相似度降低至0.9400，最终特征距离增至5.4454。

因此，本组结果不支持“只要增加不同 POOD 图片数量就一定更好”。更准确的结论是：

> 在固定500条注入记录和当前通用扰动优化方式下，5张高相似度 POOD 重复100次比10张 POOD 重复50次产生了更强的单目标影响。

这不代表多样性本身无效。如果后续为不同 POOD 分别优化扰动，或增加优化步数，`p=10` 的结果仍可能改善。

## 8. 综合结论

1. 使用 Dirichlet `alpha=1.0` 联邦模型作为代理模型后，本组实验出现了较强的目标置信度下降，6组中有2组发生标签翻转，说明 POOD 对目标模型产生影响的机制在当前设置下可以被观察到。
2. 攻击效果与 `poison_repeats` 呈明显非单调关系。重复次数75和100产生了成功攻击，而增加至200后攻击消失。
3. E4（`p=5, repeats=100, injected=500`）是当前综合表现最强的设置：目标置信度下降59.48个百分点并发生标签翻转，同时遗忘后全局准确率仍为70.87%。
4. E3（`p=5, repeats=75, injected=375`）使用更少的注入记录也实现了标签翻转，适合作为较低注入预算方案。
5. E1（`p=5, repeats=20`）虽然未翻转标签，但目标margin仅剩1.06个百分点，说明较少POOD也可能造成显著目标退化。
6. E5（`p=5, repeats=200`）表明过度复制相同POOD会削弱而非增强攻击，支持“模型记忆重复样本或训练轨迹发生偏移”的解释。
7. 当前结果仍无法单独证明攻击增强来自 Non-IID 代理模型，因为这6组中没有完全同参数的 IID 代理模型对照组。

## 9. 实验局限

- 每个参数组合目前只有一次运行，缺少均值、标准差和显著性分析。
- 当前只测试一个目标样本（index 25）和一个目标类别（bird）。
- 不同注入规模会改变恶意客户端数据量、本地批次数和 FedAvg 样本权重，因此 `poison_repeats` 同时改变了多个因素。
- 尚无“无POOD但执行相同FedEraser流程”的基线，无法完全分离 POOD 影响与 FedEraser 固有全局性能下降。
- 尚无“未扰动STL10数据”和“随机STL10数据”对照，不能量化特征优化本身贡献了多少攻击效果。
- `attack_success` 仅反映单次标签是否翻转；E3和E4的翻转margin不足1个百分点，稳定性仍需验证。
- 当前相似度只在代理模型中记录，尚未记录同一批 POOD 在最终 Non-IID 联邦模型中的特征相似度。

## 10. 下一步实验建议

### 10.1 优先验证当前最佳区间

固定 `p=5`，进一步测试：

```text
poison_repeats = 60, 75, 90, 100, 125
seed = 42, 43, 44
```

报告均值与标准差，而不是只比较一次标签翻转。重点观察：

- 目标置信度下降；
- 目标margin下降；
- 攻击成功率；
- 目标类ACC下降；
- 全局ACC下降。

### 10.2 增加三组必要对照

1. 无POOD注入，但执行相同的联邦训练与FedEraser；
2. 注入相同数量的未扰动STL10 dog数据；
3. 注入相同数量的随机STL10数据。

这三组能够判断当前目标变化究竟来自POOD特征优化、普通数据删除，还是FedEraser回放本身。

### 10.3 验证代理模型语义迁移

在同一个实验中记录目标与扰动后POOD在以下模型中的余弦相似度及Top-K排名：

```text
Non-IID代理模型
Unlearning前联邦模型
Unlearning后联邦模型
```

如果代理模型相似度很高、最终联邦模型相似度明显降低，即可支持“代理特征没有稳定迁移”的解释。

### 10.4 公平比较样本多样性

保持总注入量500，继续测试：

```text
p=5,  repeats=100
p=10, repeats=50
p=20, repeats=25
```

但对于更大的 `p`，应考虑增加扰动优化步数或使用每张图片独立扰动，否则通用扰动的优化能力会成为额外变量。

## 11. 最终建议

当前可将 `p=5, poison_repeats=100, delta_t=2` 作为后续实验基线，并将 `p=5, poison_repeats=75` 作为低注入预算对照。暂不建议继续提高到 `poison_repeats=200`。

后续最重要的工作不是继续盲目增加POOD数量，而是完成多随机种子复现、无POOD/FedEraser基线以及最终联邦模型中的特征相似度验证。完成这三项后，才能更可靠地判断POOD的定向影响强度及其语义迁移机制。
