# CIFAR10–STL10 POOD 对照实验分析报告

## 1. 实验目的

本组实验用于分离以下因素对联邦模型和目标样本预测变化的贡献：POOD数据本身、Top-K特征检索、通用扰动优化、余弦与L2两种检索度量，以及FedEraser校准回放本身。

实验为模拟测试，重点是判断目标样本和模型性能的变化究竟来自POOD，还是来自联邦训练及FedEraser近似重建误差。

## 2. 文件与实验模式映射

| 实验 | 文件 | 模式 | 检索 | 注入内容 |
|---|---|---|---|---|
| E1 | `summary(7).json` | `optimized` | 余弦 | Top-K POOD + 通用扰动 |
| E2 | `summary(6).json` | `no_pood` | 余弦 | 不注入、不删除，只执行FedEraser回放 |
| E3 | `summary(9).json` | `unperturbed` | 余弦 | Top-K POOD原图，无扰动 |
| E4 | `summary(8).json` | `random` | 余弦 | 同语义/同标签组内随机POOD原图 |
| E5 | `summary(10).json` | `optimized` | 原始L2 | L2 Top-K POOD + 通用扰动 |

## 3. 公共实验参数

| 参数 | 设置 |
|---|---|
| 联邦训练集 | CIFAR10 |
| POOD候选集 | STL10 test |
| 代理模型 | Dirichlet `alpha=1.0` CIFAR10联邦模型 |
| 代理模型准确率 | 81.92% |
| 客户端划分 | Dirichlet Non-IID，`alpha=1.0` |
| 客户端数量 | 10 |
| 联邦轮数 | 50 |
| 本地训练 | 1 epoch，学习率0.01，batch size 128 |
| 目标样本 | CIFAR10 test index 25，`bird(2)` |
| POOD客户端 | Client 6 |
| 候选数量 / Top-K | 5000 / 200 |
| 不同POOD数 | 5 |
| 注入记录数 | 500（无POOD组为0） |
| POOD语义及标签 | STL10 `dog(5)` → CIFAR10 `dog(5)` |
| FedEraser | `delta_t=2`，25个快照 |

## 4. 全局模型结果

| 实验 | 回放前ACC | 回放后ACC | ACC下降 | 回放后loss |
|---|---:|---:|---:|---:|
| E1 正常优化POOD | 81.66% | 71.96% | 9.70 pp | 0.8052 |
| E2 无POOD | 81.94% | 71.51% | 10.43 pp | 0.8171 |
| E3 无扰动POOD | 82.06% | 70.80% | 11.26 pp | 0.8225 |
| E4 随机POOD | 82.16% | 71.50% | 10.66 pp | 0.8055 |
| E5 L2检索POOD | 82.32% | 70.85% | 11.47 pp | 0.8216 |

五组训练后准确率非常接近，均处于81.66%–82.32%；但经过FedEraser回放后全部下降约9.70–11.47个百分点。

最关键的是：E2没有注入任何POOD，也没有删除任何训练记录，全局准确率仍从81.94%下降到71.51%。这说明当前FedEraser回放本身产生约10.43个百分点的基础重建误差。其他POOD实验的全局下降与这个基础误差只相差约1个百分点。

因此，现阶段不能把约10个百分点的全局下降解释为POOD遗忘效果。

## 5. 目标样本结果

| 实验 | bird置信度（前） | bird置信度（后） | 下降 | 回放后预测 | 回放后margin |
|---|---:|---:|---:|---|---:|
| E1 正常优化POOD | 73.86% | 51.65% | 22.22 pp | bird | 33.74 pp |
| E2 无POOD | 83.36% | 40.49% | 42.87 pp | bird | 17.83 pp |
| E3 无扰动POOD | 61.98% | 30.11% | 31.88 pp | bird | 8.60 pp |
| E4 随机POOD | 89.23% | 45.92% | 43.31 pp | bird | 29.79 pp |
| E5 L2检索POOD | 67.66% | 36.30% | 31.36 pp | bird | 15.59 pp |

所有实验中目标仍被预测为 `bird`。目标置信度下降最大的是随机POOD和无POOD组，而不是经过特征检索及扰动优化的POOD组。

以无POOD回放作为基础变化：

| 实验 | 目标置信度下降 | 相对无POOD基线的额外下降 |
|---|---:|---:|
| E1 正常优化POOD | 22.22 pp | **-20.65 pp** |
| E2 无POOD | 42.87 pp | 0 |
| E3 无扰动POOD | 31.88 pp | -11.00 pp |
| E4 随机POOD | 43.31 pp | +0.44 pp |
| E5 L2检索POOD | 31.36 pp | -11.51 pp |

负值表示目标下降小于无POOD回放。正常优化POOD没有产生超出FedEraser基线的目标退化；随机POOD与无POOD结果几乎相同；当前特征检索与通用扰动未表现出可辨别的额外目标作用。

需要注意，五组实验分别重新训练，回放前目标置信度并不相同，因此上述“额外下降”只能作为方向性对照，不能当作严格的因果效应估计。正式结论仍需依靠多随机种子均值和更稳定的配对实验设计。

## 6. 目标类别与其他类别变化

| 实验 | bird类别下降 | 全局下降 | bird相对全局额外下降 | 下降最大类别 |
|---|---:|---:|---:|---|
| E1 正常优化POOD | 12.0 pp | 9.70 pp | 2.30 pp | deer：21.8 pp |
| E2 无POOD | 13.2 pp | 10.43 pp | 2.77 pp | deer：26.1 pp |
| E3 无扰动POOD | 13.0 pp | 11.26 pp | 1.74 pp | frog：22.7 pp |
| E4 随机POOD | 14.0 pp | 10.66 pp | 3.34 pp | deer：27.6 pp |
| E5 L2检索POOD | 14.9 pp | 11.47 pp | 3.43 pp | deer：31.4 pp |

目标类别 `bird` 的下降与全局下降接近，没有显示正常优化POOD比对照组更强的类别定向性。无POOD时bird已下降13.2个百分点，而正常优化POOD仅下降12.0个百分点。

`deer` 在多数实验中下降最严重，且无POOD时已经下降26.1个百分点。这进一步说明当前类别变化主要由FedEraser重建误差和Non-IID训练轨迹决定，而不是由目标附近POOD单独决定。

## 7. POOD检索与扰动结果

### 7.1 正常余弦检索与优化

```text
扰动前平均目标相似度：0.8892
扰动后平均目标相似度：0.9572
平均特征距离：7.3918 → 4.6188
```

通用扰动确实完成了代理模型中的优化目标，但没有带来更大的目标预测变化。这说明“代理模型中更接近”没有转化为“最终联邦模型中更强的POOD影响”。

### 7.2 随机POOD

随机POOD平均目标相似度只有0.7968，明显低于Top-K原图的0.8892和优化后的0.9572，但目标置信度下降43.31个百分点，反而大于正常优化POOD的22.22个百分点。

当前目标变化与代理模型相似度不呈正相关。随机POOD的较大变化更可能来自训练随机性和FedEraser基础误差，而不是其特征质量。

### 7.3 余弦与L2检索

余弦检索索引：

```text
1232, 5784, 5942, 1869, 7972
```

L2检索索引：

```text
5784, 1232, 7972, 5942, 134
```

两者有4/5样本重合，重合率80%，且都选择 `dog`。

| 指标 | 余弦 | L2 |
|---|---:|---:|
| 扰动前余弦相似度 | 0.8892 | 0.8890 |
| 扰动后余弦相似度 | 0.9572 | 0.9555 |
| 扰动后特征距离 | 4.6188 | 4.7021 |
| 目标置信度下降 | 22.22 pp | 31.36 pp |
| 全局ACC下降 | 9.70 pp | 11.47 pp |

两种检索的邻域基本相同，代理特征指标也非常接近。L2实验中更大的目标下降同时伴随更大的全局下降，且仍低于无POOD目标下降，因此没有证据表明L2优于余弦。检索度量不是当前主要瓶颈。

## 8. 遗忘集结果

| 实验 | 遗忘/探针集ACC（前） | 之后ACC | 说明 |
|---|---:|---:|---|
| E1 正常优化POOD | 100% | 0% | 注入记录删除后预测显著变化 |
| E2 无POOD | 0% | 20% | 仅为probe，不是遗忘集 |
| E3 无扰动POOD | 100% | 20% | 注入记录删除后预测显著变化 |
| E4 随机POOD | 100% | 40% | 注入记录删除后预测变化较弱 |
| E5 L2检索POOD | 100% | 0% | 注入记录删除后预测显著变化 |

FedEraser确实改变了模型对被删除POOD本身的预测，但遗忘集变化没有稳定传递到目标样本。遗忘集从100%降至0%不能证明目标样本受到特定影响。

## 9. 问题定位

### 9.1 FedEraser回放误差主导结果

无POOD、无删除时，全局准确率下降10.43个百分点，目标置信度下降42.87个百分点。这是当前最严重的问题。只要基础回放误差大于POOD带来的增量，就无法可靠识别POOD贡献。

### 9.2 代理特征相似度没有证明能够迁移

优化使代理相似度从0.8892升至0.9572，但正常优化POOD的目标变化反而最小。当前只记录代理模型特征，没有记录相同POOD在最终联邦模型中的特征位置，核心语义迁移假设尚未验证。

### 9.3 单目标对训练轨迹非常敏感

五组全局训练准确率仅相差0.66个百分点，但同一目标在回放前的置信度从61.98%变化到89.23%。单个目标对POOD内容、客户端本地批次数以及CUDA训练波动非常敏感，单次运行不足以支持方法优劣结论。

### 9.4 通用扰动没有显示额外收益

无扰动POOD目标下降31.88个百分点，优化POOD仅下降22.22个百分点。虽然不能根据单次运行断言扰动有负作用，但本组结果不支持“代理相似度越高，目标影响越强”。

### 9.5 余弦与L2差异很小

两种方法80%的样本相同，选择相同类别，代理优化结果接近。检索度量改变没有触及当前主要问题。

## 10. 下一步实验建议

### 第一优先级：修复或校准无POOD回放基线

只运行 `no_pood`，测试：

```text
delta_t = 1, 2, 5
calibration_learning_rate = 0.001, 0.005, 0.01
calibration_local_epochs = 1, 2
```

记录全局ACC、各类别ACC、目标logit/置信度/margin、原模型与回放模型输出分布的KL散度及预测一致率。建议先把无POOD全局ACC下降控制到2–3个百分点以内，再继续解释POOD实验。

### 第二优先级：验证最终联邦模型中的特征迁移

对同一目标和同一批POOD，在代理模型、回放前联邦模型和回放后联邦模型中分别计算余弦相似度、L2距离和Top-K排名。

如果代理相似度0.957，但最终联邦模型相似度明显降低，即可确认POOD优化过拟合代理模型或语义邻域没有迁移。

### 第三优先级：多随机种子和多目标

固定：

```text
p = 5
poison_repeats = 100
delta_t = 经过无POOD基线验证后的最佳值
```

至少测试 `seed=42,43,44`，每个目标类别选择低、中、高margin目标各1个，并报告均值、标准差和预测变化率。

### 第四优先级：重新比较扰动优化

在FedEraser基线稳定后，再比较无扰动POOD、当前通用L2扰动、余弦目标通用扰动和多代理模型联合扰动。判断标准应是“相对无POOD基线的额外变化”，而不是单次前后差值。

### 第五优先级：降低余弦/L2比较优先级

当前两者80%样本重合，差异被FedEraser误差和单次波动覆盖。可保留为最终消融，但不应作为下一阶段主要优化方向。

## 11. 推荐的最小实验路线

1. 运行 `no_pood + delta_t=1`，检查回放是否接近原模型；
2. 固定最佳回放参数，用3个随机种子重复 `no_pood`；
3. 在相同参数下比较 `no_pood`、`unperturbed`、`optimized`；
4. 比较POOD在代理模型与最终联邦模型中的特征相似度；
5. 扩展到多个目标样本；
6. 最后完成余弦与L2检索消融。

## 12. 总结

本组对照实验定位出当前主要问题：模型变化主要由FedEraser近似回放误差产生，而不是由POOD检索或扰动优化产生。无POOD条件下已经出现10.43个百分点的全局准确率下降和42.87个百分点的目标置信度下降，超过或接近所有POOD实验。

代理模型中的高特征相似度没有转化为更强的目标影响；随机POOD相似度最低却产生与无POOD几乎相同的目标变化。余弦与L2检索得到80%相同样本，说明检索度量不是当前瓶颈。

下一步不应继续增加POOD数量或优先调整检索距离，而应先改善无POOD FedEraser回放质量，并验证POOD特征是否真正迁移到最终联邦模型。只有基础回放稳定后，才能可靠评估POOD数据对目标样本和模型性能的额外影响。
